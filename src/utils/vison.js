/**
 * PD * distance/(PD + disparty)
 * @param {Number} disparty 视差
 * @param {Number} distance 距离
 * @param {Number} PD 瞳距
 * @param {mode} mode 瞳距
 * 参数单位皆为毫米
 */
export function vergence(disparty = 0, distance = 400, PD = 60, mode = 'RAD' ) {
    return
}